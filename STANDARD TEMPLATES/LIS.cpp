#include<iostream>

typedef long long int ll;

using namespace std;

//RECURSIVE SOLUTION
int lis(ll a,ll start,ll end, ll max){

	//After reaching end of the given list
	if(start > end) return 0;

	//Cases for Excluding current element 
	//1. Start new sequence with curr element as starting element
	//2. Include the curr element

	lis1 = lis(all,start+1,end,max);

	//
}

int main(){

}